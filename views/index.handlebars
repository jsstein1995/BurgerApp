{{!-- <div class="container">
    <div class="row">
        <div class="col-xs-12 text-center" id="top-bar">
            <h1>Eat Da Burger!</h1>
        </div>
        <div class="col-xs-6 text-center" class="task">
            <div class="row">
                {{#each burgers}}
                {{#unless this.devoured}}
                <div class="col-xs-9 text-center">
                    <pre>{{this.id}}. {{this.burger_name}}</pre>
                </div>
                <div class="col-xs-3 text-center">
                    <form method="POST" action="/burgers/update?_method=PUT" class="button-size">
                        <input type="hidden" name="burger_id" type="text" value={{this.id}}>
                        <br>
                        <button type="submit" class="btn btn-default">
                            DEVOUR IT!
                        </button>
                    </form>
                </div>
                {{/unless}}
                {{/each}}
            </div>
        </div>

        <div class="col-xs-6 text-center" class="task">
            {{#each burger_data}}
            {{#if this.devoured}}
            <input class="form-control" type="text" placeholder="{{this.id}}. {{this.burger_name}}" readonly>
            {{/if}}
            {{/each}}
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-xs-12 text-center ender_section">
            <form action="/burgers/create" method="POST">
                <input type="text" name="burger_name" class="form-control" value="" id="enter_text">
                <button type="submit" class="btn btn-primary" id="text-enter-button">SUBMIT</button>
            </form>
            <p>Enter in a burger you would like to eat!</p>
        </div>
    </div>
</div> --}}


<div class="container">

    <div class='jumbotron heading'>
        <h1>{{title}}</h1>

    </div>
    <div class="i-am-centered">
        <div class="row">

            <h1>Add a Burger</h1>

            <form>
                <input type='text' id='name'>
                <button class='btn btn-success' id='submitName'>Submit Burger</button>
            </form>
        </div>
    </div>

    <main>

        <div class="col-sm-6">
            <h1>Available Burgers:</h1>
            {{#each burger}}
            {{#unless this.devoured}}
            <h3>{{this.id}} - {{this.burger_name}} <button class='devour btn btn-info' data-id="{{this.id}}"> Devour
                    it</button> </h3>
            {{/unless}}
            {{/each}}
        </div>

        <div class="col-sm-6">
            <h1>Devoured Burgers:</h1>
            {{#each burger}}
            {{#if this.devoured}}
            <div class='burgerEaten'>
                <div>
                    <h3> {{this.id}} - {{this.burger_name}} <button class='delete btn btn-danger' data-id="{{this.id}}">
                            X</button></h3>
                </div>
            </div>
            {{/if}}
            {{/each}}
        </div>
</div>

</main>